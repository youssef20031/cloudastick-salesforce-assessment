<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>abc_Line_Items_JSON__c</fullName>
    <description>The order's line items serialised as JSON - product name, product code, quantity, unit price and line total. Big objects support neither master-detail nor lookup relationships, so keeping the items inside the parent row is what makes an archived order atomic: one insertImmediate either archives the whole order or none of it.</description>
    <label>Line Items JSON</label>
    <length>131072</length>
    <required>false</required>
    <type>LongTextArea</type>
    <visibleLines>10</visibleLines>
</CustomField>
