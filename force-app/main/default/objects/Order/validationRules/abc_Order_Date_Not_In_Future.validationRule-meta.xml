<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>abc_Order_Date_Not_In_Future</fullName>
    <active>true</active>
    <description>An order cannot be placed in the future. Keeps the one-year archival window honest, since the batch ages orders against Order Date.</description>
    <errorConditionFormula>NOT(ISBLANK(abc_Order_Date__c)) &amp;&amp; abc_Order_Date__c &gt; TODAY()</errorConditionFormula>
    <errorDisplayField>abc_Order_Date__c</errorDisplayField>
    <errorMessage>Order Date cannot be in the future.</errorMessage>
</ValidationRule>
